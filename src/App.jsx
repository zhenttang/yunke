import React from 'react';
import LandingDesktop from './components/LandingDesktop';

// Unified entry: reuse the desktop experience on all viewports for consistent visuals.
const App = () => <LandingDesktop />;

export default App;
